export const searchHelpText =
  'Enter any part of the data to search across all columns. Use a comma to separate multiple search terms. For example, "term1, term2". The search is case-insensitive. The search is an AND search. For example, "term1, term2" will return rows that contain both "term1" and "term2".';
